# journal/

Daily notes. Format not yet defined — will be defined the first time this
folder is actually used (e.g. a dated `YYYY-MM-DD.md` per day). Distinct
from `wiki/log.md`: the log is a terse append-only activity record; journal
entries (once defined) would be longer-form daily notes/reflections.

Empty for now.
