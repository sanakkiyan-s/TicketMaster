# Copy to .env before running docker-compose. Never commit the real .env.

POSTGRES_USER=ticketmaster
POSTGRES_PASSWORD=ticketmaster
POSTGRES_DB=ticketmaster

VAULT_DEV_ROOT_TOKEN=dev-root-token

MINIO_ROOT_USER=ticketmaster
MINIO_ROOT_PASSWORD=ticketmaster123
